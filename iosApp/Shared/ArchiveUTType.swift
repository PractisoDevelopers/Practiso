import UniformTypeIdentifiers

extension UTType {
    public static var psarchive: UTType { UTType(exportedAs: "com.zhufucdev.psarchive") }
    public static var psquiz: UTType { UTType(exportedAs: "com.zhufucdev.psquiz") }
}
